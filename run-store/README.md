# Immutable Run Store

This is the generated opaque-ID view over immutable evidence. `run-store/generated/runs/YYYY/MM/<run_uid>` points to the compatibility execution path under `backtest-campaigns/`.

The compatibility path remains authoritative until a separately reviewed physical migration updates every writer, provenance pointer, and integrity check. Rebuild this store with `make run-store`.
