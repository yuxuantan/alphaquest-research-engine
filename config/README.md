# Repository Configuration

This directory contains repository-wide research settings and policy inputs. Campaign-specific mechanics belong under `campaigns/`; instrument reference data belongs under `data/reference/`.

Changes to stage order, thresholds, or methodology policy require tests and a durable methodology note.
