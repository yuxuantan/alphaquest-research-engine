# Research Studio drafts

Autosaved, incomplete Studio drafts live in one subdirectory per campaign. Drafts are outside active campaign discovery and are never executable evidence. Only the transactional publisher may compile a frozen, valid draft into `research/campaigns/active/`.
