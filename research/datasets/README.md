# Governed Studio datasets

Each imported dataset has a strict `dataset_manifest.json` and canonical completed bars. Original local attachments are kept separately under the ignored Studio runtime quarantine. A dataset that is not `PASS` cannot be compiled into a Studio campaign.
