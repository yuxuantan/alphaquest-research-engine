# first30_signed_flow_continuation_1000 rescue1

This is the single allowed parameter-space rescue for `first30_signed_flow_continuation_1000`.

Allowed change: Original limited-core winners were concentrated at stricter return/flow thresholds and the widest stop; rescue narrows only those existing parameter neighborhoods.

Controls: entry module, source window, flow mode, direction mode, stop module, target module, data, costs, slippage, sessions, same-bar handling, and validation gates are unchanged. Target R grid remains `[1.0, 1.5, 2.0]`; no sub-1R target is allowed.
