# Strategy Modules

Entry: `profile_aoi_footprint_trap`

Stop-loss: `sweep_extreme`

Take-profit / exit: `cost_adjusted_fixed_r` plus forced flatten at `15:55:00` ET.

Mechanic: Trade either side of an opening-range boundary trap through the day, but only when ORH/ORL is near a prior profile level and the completed footprint bar shows trapped aggression.
