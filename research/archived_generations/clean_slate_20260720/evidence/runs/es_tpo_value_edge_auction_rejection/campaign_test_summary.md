# ES TPO Value-Edge Auction Rejection — Campaign Result

**FAIL.** All five variants failed the official limited-core grid stage. None of 40 parameter combinations was profitable or passed benchmarks, and no variant reached the required 156 trades/year. Later WFA, monkey, Monte Carlo, incubation, and acceptance stages were skipped by the fail-closed pipeline.

See `campaign_results.csv` for the shortlist and full-history fixed-configuration metrics.
