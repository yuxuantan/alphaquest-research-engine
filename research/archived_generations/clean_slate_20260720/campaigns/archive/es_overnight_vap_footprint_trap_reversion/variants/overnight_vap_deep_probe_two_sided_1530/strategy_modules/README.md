# Strategy Modules

This variant uses `profile_aoi_footprint_trap` for entry, `sweep_extreme` for stop loss, and `cost_adjusted_fixed_r` for take profit. The source implementations live under `src/alphaquest/strategy_modules/`; this folder records the module contract required by the campaign artifact policy.
