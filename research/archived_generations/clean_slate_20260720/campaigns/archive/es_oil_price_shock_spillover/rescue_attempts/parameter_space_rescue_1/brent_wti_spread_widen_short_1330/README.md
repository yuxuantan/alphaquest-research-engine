# brent_wti_spread_widen_short_1330 rescue1

Scope: one allowed parameter-space rescue for failed original `brent_wti_spread_widen_short_1330`.

Mechanic retained: same EIA two-business-day lag, setup mode, direction, entry time, entry module, stop module, target module, data window, costs, fills, flatten time, and validation gates.

Allowed changes used: entry threshold grid, fixed threshold default, stop grid, and target grid only.
