# Strategy Modules: mon_fri_polarity_pair_1000

Entry uses `calendar_session_bias`; stop uses `percent_from_entry`; target uses `fixed_r`. The local file `entry.py` re-exports the production entry class for audit traceability.
