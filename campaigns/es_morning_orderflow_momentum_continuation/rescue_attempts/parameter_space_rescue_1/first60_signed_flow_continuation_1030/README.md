# first60_signed_flow_continuation_1030 rescue1

This is the single allowed parameter-space rescue for `first60_signed_flow_continuation_1030`.

Allowed change: Original best combinations used the stricter signed-flow corner and widest stop; rescue preserves the first-hour signed-flow continuation mechanic and narrows the threshold space without adding filters.

Controls: entry module, source window, flow mode, direction mode, stop module, target module, data, costs, slippage, sessions, same-bar handling, and validation gates are unchanged. Target R grid remains `[1.0, 1.5, 2.0]`; no sub-1R target is allowed.
