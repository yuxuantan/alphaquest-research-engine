# first45_large10_flow_continuation_1015 rescue1

This is the single allowed parameter-space rescue for `first45_large10_flow_continuation_1015`.

Allowed change: Original results were only marginally positive at the strict large10 corner, so rescue keeps the same 45-minute large-trade-flow mechanic and tests a tighter entry neighborhood with a slightly wider stop grid.

Controls: entry module, source window, flow mode, direction mode, stop module, target module, data, costs, slippage, sessions, same-bar handling, and validation gates are unchanged. Target R grid remains `[1.0, 1.5, 2.0]`; no sub-1R target is allowed.
