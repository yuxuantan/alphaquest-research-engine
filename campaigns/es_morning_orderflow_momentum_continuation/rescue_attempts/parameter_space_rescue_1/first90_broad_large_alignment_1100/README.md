# first90_broad_large_alignment_1100 rescue1

This is the single allowed parameter-space rescue for `first90_broad_large_alignment_1100`.

Allowed change: Original was broadly negative, but the required per-failed-variant rescue keeps the same first-90 broad-plus-large alignment mechanic and only tests a slightly stricter return neighborhood with wider stops.

Controls: entry module, source window, flow mode, direction mode, stop module, target module, data, costs, slippage, sessions, same-bar handling, and validation gates are unchanged. Target R grid remains `[1.0, 1.5, 2.0]`; no sub-1R target is allowed.
