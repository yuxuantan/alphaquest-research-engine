# dax_1d_strength_long_1330

This variant reuses repository entry module `nq_europe_equity_close_spillover`, stop module `percent_from_entry`, and take-profit module `fixed_r`. The European index features are same-day only and are treated as available no earlier than 13:30 ET.
