# ovap60_large_value_accept_1500

Entry module: `opening_vap_large_record_reaction`.

Mechanic: Value-area acceptance through opening60 VAH/VAL with same-side large-200 record proxy and completed-bar footprint imbalance.

Stop module: `sweep_extreme`.

Take-profit module: `cost_adjusted_fixed_r`.
