# or15_signed_pivot_flow_breakout_1030

15-minute signed-flow opening-range breakout filtered by completed 5/15-minute pivot direction.

The base opening-range orderflow breakout must trigger first. The completed pivot filter is fixed before testing and accepts only same-direction confirmed market structure; it does not add a tunable parameter.


## Parameter-space rescue 1

Same mechanics and modules. Entry buffer/imbalance space is shifted less strict, stop cap gets a wider option, and targets remain >= 1.0R.
