# afternoon_large20_two_sided_acceptance rescue1

Rescue attempt 1 keeps the original prior value-area acceptance mechanic. Original afternoon best rows required the widest original stop and larger target; rescue keeps the same large-20 acceptance mechanic and tests adjacent wider-risk continuation geometry.

Allowed changes only: `entry.params.breakout_buffer_ticks`, `entry.params.min_orderflow_imbalance`, `sl.params.stop_pct`, and `tp.params.target_r_multiple` parameter spaces.
