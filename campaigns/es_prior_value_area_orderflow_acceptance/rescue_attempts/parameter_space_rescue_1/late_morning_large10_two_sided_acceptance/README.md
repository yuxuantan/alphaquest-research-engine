# late_morning_large10_two_sided_acceptance rescue1

Rescue attempt 1 keeps the original prior value-area acceptance mechanic. Original late-morning two-sided grid was closest with modest targets; rescue keeps the same large-10 acceptance mechanic and narrows around the adjacent stop/target neighborhood.

Allowed changes only: `entry.params.breakout_buffer_ticks`, `entry.params.min_orderflow_imbalance`, `sl.params.stop_pct`, and `tp.params.target_r_multiple` parameter spaces.
