# Authored Campaigns

Each directory represents one economic edge. Read and edit authored `campaign.yaml`, variant configs, and explicitly authorized rescue definitions here. Generated backtest results belong under `backtest-campaigns/` and must not be copied back as source.

Use `propstack research search` to find campaigns and `propstack campaign show <campaign_id>` to inspect one without browsing all directories.
