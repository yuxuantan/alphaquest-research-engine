# midday_signed_two_sided_magnet

Two-sided midday variant. It tests whether the prior-session approximate POC remains a liquidity magnet after the first two hours, using broad signed-volume imbalance as the participation filter.
