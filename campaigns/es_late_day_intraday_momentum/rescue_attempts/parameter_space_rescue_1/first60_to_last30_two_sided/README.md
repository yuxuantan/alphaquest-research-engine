# first60_to_last30_two_sided

This variant measures the early-session return over the first 60 RTH minutes instead of the first 30, then trades the same direction at the 15:30 ET late-day window start. Both long and short directions are allowed.

Tunable parameters are fixed before testing: one entry threshold, one percent stop, and one fixed-R target.
