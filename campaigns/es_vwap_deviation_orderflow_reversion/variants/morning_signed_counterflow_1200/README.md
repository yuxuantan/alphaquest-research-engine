# morning_signed_counterflow_1200

Morning two-sided VWAP-deviation reversion with signed-volume counterflow through 12:00 ET.

Entry: `vwap_deviation_orderflow_reversion`. Stop: `sweep_extreme`. Target: `fixed_r`.
