# late_morning_signed_up_extension_short_1130 rescue1

Single allowed parameter-space rescue. Same morning extension plus counterflow reversal mechanic; only stricter extension/counterflow thresholds and smaller fixed-R targets are tested.
