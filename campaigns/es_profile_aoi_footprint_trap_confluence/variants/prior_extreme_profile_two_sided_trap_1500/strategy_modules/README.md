# Strategy Modules

Entry: `profile_aoi_footprint_trap`

Stop-loss: `sweep_extreme`

Take-profit / exit: `cost_adjusted_fixed_r` plus forced flatten at `15:55:00` ET.

Mechanic: Trade either a prior RTH low seller trap long or prior RTH high buyer trap short only when the prior extreme is near a prior-session profile level and the completed footprint bar closes back away from trapped aggression.
