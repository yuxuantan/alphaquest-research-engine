# Strategy Modules

This variant uses `true_vap_aoi_breakout_continuation` for entry, `sweep_extreme` for stop loss, and `cost_adjusted_fixed_r` for take profit. Source implementations live under `src/propstack/strategy_modules/`.
