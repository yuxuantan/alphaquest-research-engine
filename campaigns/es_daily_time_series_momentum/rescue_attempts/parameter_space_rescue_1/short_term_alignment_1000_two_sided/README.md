# short_term_alignment_1000_two_sided

This variant requires short-term and intermediate ES close-to-close trend alignment before taking the 10:00 ET trade. The signal uses prior completed RTH closes only, enters on the next bar open, and flattens same day.

Tunable parameters are fixed before testing: two entry parameters, one percent stop, and one fixed-R target.
