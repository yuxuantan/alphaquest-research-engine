# Strategy modules

- Entry: `pdh_pdl_orderflow_breakout_continuation` from `src/propstack/strategy_modules/entry/`.
- Stop loss: `prior_level_retest_boundary` from `src/propstack/strategy_modules/sl/`.
- Take profit / exit: `fixed_r` from `src/propstack/strategy_modules/tp/`.
- All logic is shared repo module code; this directory records the module contract for the campaign artifact.
