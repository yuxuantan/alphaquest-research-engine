# afternoon_60m_impulse_reversal_1400 rescue1

This is the single allowed rescue for the failed original variant. It preserves the `orderflow_regime` flow-impulse-reversal entry, `sweep_extreme` stop, `fixed_r` target, signal time, data, costs, fills, validation gates, and core economic thesis. Only the stop-offset and target-R parameter spaces are shifted toward a quicker micro-retracement expression.
